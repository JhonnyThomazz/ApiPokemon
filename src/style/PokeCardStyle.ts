import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    card: {
        backgroundColor: "#ffffff",
        borderRadius: 12,
        padding: 20,
        marginTop: 25,
        elevation: 4,
    },
    titulo: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 15,
    },
});